// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: user.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const getUserById = `-- name: GetUserById :one
SELECT id, iid, email, name, avatar_url, provider, provider_id, created_at
FROM users
WHERE "id" = $1
`

func (q *Queries) GetUserById(ctx context.Context, id int32) (User, error) {
	row := q.db.QueryRow(ctx, getUserById, id)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Iid,
		&i.Email,
		&i.Name,
		&i.AvatarUrl,
		&i.Provider,
		&i.ProviderID,
		&i.CreatedAt,
	)
	return i, err
}

const getUserIidById = `-- name: GetUserIidById :one
SELECT iid
FROM users
WHERE "id" = $1
`

func (q *Queries) GetUserIidById(ctx context.Context, id int32) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, getUserIidById, id)
	var iid uuid.UUID
	err := row.Scan(&iid)
	return iid, err
}

const updateOrCreateUser = `-- name: UpdateOrCreateUser :one
INSERT INTO users(email, name, avatar_url, provider, provider_id)
    VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (email)
    DO UPDATE SET
        name = EXCLUDED.name,
        avatar_url = EXCLUDED.avatar_url,
        provider = EXCLUDED.provider,
        provider_id = EXCLUDED.provider_id
    RETURNING id, iid, email, name, avatar_url, provider, provider_id, created_at
`

type UpdateOrCreateUserParams struct {
	Email      string
	Name       string
	AvatarUrl  *string
	Provider   Provider
	ProviderID string
}

func (q *Queries) UpdateOrCreateUser(ctx context.Context, arg UpdateOrCreateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, updateOrCreateUser,
		arg.Email,
		arg.Name,
		arg.AvatarUrl,
		arg.Provider,
		arg.ProviderID,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Iid,
		&i.Email,
		&i.Name,
		&i.AvatarUrl,
		&i.Provider,
		&i.ProviderID,
		&i.CreatedAt,
	)
	return i, err
}
